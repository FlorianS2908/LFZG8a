{
  "name": "container-factory-lab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cd ../content-factory-lab && npm run dev",
    "build": "cd ../content-factory-lab && npm run build",
    "test": "cd ../content-factory-lab && npm test"
  }
}
