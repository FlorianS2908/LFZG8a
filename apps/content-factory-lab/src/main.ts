// Browser-MVP entrypoint is implemented in main.js so the lab starts without a build step.
// This TypeScript entrypoint marks the intended migration target for a later Vite/React setup.
export const contentFactoryLabEntrypoint = 'src/main.js';
