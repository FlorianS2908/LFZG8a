# PLOGLAN API

Platzhalter fuer die spaetere zentrale API der SaaS-Plattform.

In diesem Schritt wird hier noch keine produktive API gebaut. Repository-Interfaces und Domain-Services liegen in `packages/`.
